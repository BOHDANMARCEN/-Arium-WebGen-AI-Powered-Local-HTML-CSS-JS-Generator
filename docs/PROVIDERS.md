# 🧠 Документація провайдерів AI

Цей документ описує налаштування та використання різних AI-провайдерів у Arium WebGen.

## 📋 Зміст

- [Локальні провайдери](#локальні-провайдери)
- [Хмарні провайдери](#хмарні-провайдери)
- [Налаштування](#налаштування)
- [Troubleshooting](#troubleshooting)

## 🖥 Локальні провайдери

### Ollama

Ollama дозволяє запускати великі мовні моделі локально на вашому комп'ютері.

#### Встановлення

1. Завантажте та встановіть [Ollama](https://ollama.ai/)
2. Запустіть Ollama сервер
3. Завантажте модель:

```bash
ollama pull llama2
# або
ollama pull mistral
# або
ollama pull codellama
```

#### Налаштування

Додайте до `.env.local`:

```env
OLLAMA_API_BASE=http://localhost:11434
DEFAULT_PROVIDER=ollama
```

#### Рекомендовані моделі

- `llama2` - Загальна модель
- `mistral` - Швидка та ефективна
- `codellama` - Спеціалізована для коду
- `qwen2.5` - Thinking модель з міркуваннями

### LM Studio

LM Studio - це інтерфейс для локальних LLM моделей з простим API.

#### Встановлення

1. Завантажте та встановіть [LM Studio](https://lmstudio.ai/)
2. Завантажте модель через інтерфейс
3. Запустіть локальний сервер (кнопка "Start Server")

#### Налаштування

Додайте до `.env.local`:

```env
LM_STUDIO_API_BASE=http://localhost:1234/v1
DEFAULT_PROVIDER=lm_studio
```

#### Рекомендовані моделі

- `mistral-7b-instruct`
- `llama-2-7b-chat`
- `codellama-7b-instruct`
- `qwen2.5-7b-instruct` - Thinking модель

## ☁️ Хмарні провайдери

### DeepSeek

DeepSeek - це хмарний AI-сервіс з конкурентними цінами.

#### Отримання API ключа

1. Зареєструйтеся на [DeepSeek](https://www.deepseek.com/)
2. Перейдіть до розділу API
3. Створіть новий API ключ

#### Налаштування

Додайте до `.env.local`:

```env
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
DEFAULT_PROVIDER=deepseek
```

#### Доступні моделі

- `deepseek-chat` - Загальна модель
- `deepseek-coder` - Спеціалізована для коду

### Custom OpenAI-compatible API

Підтримуються будь-які OpenAI-сумісні API, включаючи:

- OpenAI
- Together AI
- Anyscale
- Groq
- Claude AI (через OpenAI-сумісний інтерфейс)
- Anthropic (через OpenAI-сумісний інтерфейс)

#### Налаштування OpenAI

```env
OPENAI_COMPATIBLE_API_KEY=sk-your-key-here
OPENAI_COMPATIBLE_API_BASE=https://api.openai.com/v1
DEFAULT_PROVIDER=openai_compatible
```

#### Налаштування Together AI

```env
OPENAI_COMPATIBLE_API_KEY=your-together-api-key
OPENAI_COMPATIBLE_API_BASE=https://api.together.xyz/v1
DEFAULT_PROVIDER=openai_compatible
```

#### Налаштування Groq

```env
OPENAI_COMPATIBLE_API_KEY=your-groq-api-key
OPENAI_COMPATIBLE_API_BASE=https://api.groq.com/openai/v1
DEFAULT_PROVIDER=openai_compatible
```

## ⚙️ Налаштування

### Перемикання провайдерів

Ви можете змінити провайдера в інтерфейсі або через змінну оточення:

```env
DEFAULT_PROVIDER=ollama  # або lm_studio, deepseek, openai_compatible
```

### Налаштування моделей

Моделі автоматично завантажуються з API провайдера. Для локальних провайдерів переконайтеся, що модель завантажена:

**Ollama:**
```bash
ollama list  # Перевірка завантажених моделей
ollama pull model-name  # Завантаження моделі
```

**LM Studio:**
- Відкрийте LM Studio
- Завантажте модель через інтерфейс
- Переконайтеся, що сервер запущений

### Thinking Models

Thinking моделі (наприклад, Qwen, DeepCoder) показують процес міркування перед генерацією коду.

Для використання:
1. Виберіть thinking модель
2. Оберіть "Thinking" системний промпт
3. Під час генерації ви побачите процес міркування

## 🔧 Troubleshooting

### Проблеми з підключенням

#### Ollama

**Помилка:** "Cannot connect to Ollama"

**Рішення:**
1. Переконайтеся, що Ollama запущений
2. Перевірте, що порт 11434 відкритий
3. Спробуйте: `curl http://localhost:11434/api/tags`

#### LM Studio

**Помилка:** "Cannot connect to LM Studio"

**Рішення:**
1. Переконайтеся, що LM Studio запущений
2. Перевірте, що локальний сервер активний (кнопка "Start Server")
3. Перевірте порт 1234

#### Хмарні провайдери

**Помилка:** "API Key is incorrect"

**Рішення:**
1. Перевірте правильність API ключа
2. Перевірте базовий URL
3. Переконайтеся, що у вас є достатньо кредитів/квот

### Проблеми з моделями

**Модель не відображається:**

1. Для локальних провайдерів - переконайтеся, що модель завантажена
2. Для хмарних - перевірте доступність моделі в API
3. Перезапустіть сервер розробки

**Модель генерує помилки:**

1. Спробуйте іншу модель
2. Перевірте системний промпт
3. Зменшіть max tokens

### Оптимізація продуктивності

#### Локальні моделі

- Використовуйте менші моделі для швидшої генерації
- Збільште RAM для більших моделей
- Використовуйте GPU для прискорення

#### Хмарні провайдери

- Використовуйте швидкі моделі (наприклад, Groq)
- Налаштуйте max tokens для контролю часу відповіді
- Кешуйте результати там, де можливо

## 📚 Додаткові ресурси

- [Ollama Documentation](https://github.com/ollama/ollama)
- [LM Studio Documentation](https://lmstudio.ai/docs)
- [DeepSeek API Documentation](https://platform.deepseek.com/api-docs/)
- [OpenAI API Documentation](https://platform.openai.com/docs)

## 💡 Поради

1. **Для швидкої генерації:** Використовуйте Groq або менші локальні моделі
2. **Для якості коду:** Використовуйте спеціалізовані моделі (codellama, deepseek-coder)
3. **Для розуміння процесу:** Використовуйте thinking моделі (qwen2.5)
4. **Для приватності:** Використовуйте локальні провайдери (Ollama, LM Studio)

