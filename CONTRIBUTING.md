# 🤝 Внесок у проєкт Arium WebGen

Дякуємо за інтерес до внеску в Arium WebGen! Цей документ містить інструкції та рекомендації для контриб'юторів.

## 📋 Зміст

- [Кодекс поведінки](#кодекс-поведінки)
- [Як почати](#як-почати)
- [Процес розробки](#процес-розробки)
- [Стандарти коду](#стандарти-коду)
- [Надсилання змін](#надсилання-змін)
- [Структура проєкту](#структура-проєкту)

## 📜 Кодекс поведінки

### Наші обіцянки

Як учасники та лідери, ми зобов'язуємося зробити участь у нашій спільноті бездосвідним та приємним для всіх, незалежно від віку, розміру тіла, видимої чи невидимої інвалідності, етнічної приналежності, статі, гендерної ідентичності та виразу, рівня досвіду, освіти, соціально-економічного статусу, національності, особистого зовнішнього вигляду, раси, релігії або сексуальної ідентичності та орієнтації.

### Очікувана поведінка

- Використовуйте привітну та інклюзивну мову
- Поважайте різні точки зору та досвід
- Вітайте конструктивну критику
- Зосереджуйтесь на тому, що найкраще для спільноти
- Проявляйте емпатію до інших членів спільноти

## 🚀 Як почати

### Передумови

- Node.js 20+
- npm, yarn або pnpm
- Git
- Базове розуміння React та Next.js

### Налаштування середовища розробки

1. **Fork та клонуйте репозиторій:**

```bash
git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/-Arium-WebGen-AI-Powered-Local-HTML-CSS-JS-Generator.git
cd -Arium-WebGen-AI-Powered-Local-HTML-CSS-JS-Generator
```

2. **Встановіть залежності:**

```bash
npm install
```

3. **Створіть `.env.local`:**

```env
DEFAULT_PROVIDER=lm_studio
OLLAMA_API_BASE=http://localhost:11434
LM_STUDIO_API_BASE=http://localhost:1234/v1
```

4. **Запустіть dev сервер:**

```bash
npm run dev
```

## 🔄 Процес розробки

### Створення нової гілки

```bash
git checkout -b feature/назва-фічі
# або
git checkout -b fix/назва-бага
# або
git checkout -b docs/назва-документації
```

### Назви гілок

Використовуйте префікси:
- `feature/` - для нових функцій
- `fix/` - для виправлень помилок
- `docs/` - для документації
- `refactor/` - для рефакторингу
- `test/` - для тестів
- `chore/` - для рутинних завдань

### Коміти

Використовуйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: додано підтримку нового провайдера
fix: виправлено помилку з генерацією коду
docs: оновлено README
refactor: рефакторинг компонента ProviderSelector
test: додано тести для API routes
chore: оновлено залежності
```

## 📝 Стандарти коду

### TypeScript

- Використовуйте TypeScript для всіх нових файлів
- Додавайте типи для всіх функцій та змінних
- Уникайте `any` - використовуйте `unknown` або конкретні типи

### React

- Використовуйте функціональні компоненти з хуками
- Дотримуйтесь правил хуків React
- Використовуйте `use client` директиви там, де потрібно

### Стиль коду

- Використовуйте Prettier для форматування
- Дотримуйтесь ESLint правил
- Використовуйте meaningful назви змінних та функцій

### Приклад хорошого коду

```typescript
// ✅ Добре
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
}

export function getProviderConfig(id: string): ProviderConfig | null {
  // implementation
}

// ❌ Погано
function getConfig(id: any): any {
  // implementation
}
```

## 📤 Надсилання змін

### Pull Request процес

1. **Оновіть вашу гілку:**

```bash
git checkout main
git pull upstream main
git checkout ваша-гілка
git rebase main
```

2. **Переконайтеся, що код працює:**

```bash
npm run lint
npm run build
```

3. **Створіть Pull Request:**

- Заповніть шаблон PR
- Додайте опис змін
- Вкажіть пов'язані issues
- Додайте скріншоти, якщо це UI зміни

### Шаблон Pull Request

```markdown
## Опис
Короткий опис змін

## Тип змін
- [ ] Нова функція
- [ ] Виправлення помилки
- [ ] Документація
- [ ] Рефакторинг
- [ ] Інше

## Чеклист
- [ ] Код відповідає стандартам проєкту
- [ ] Я перевірив код самостійно
- [ ] Я додав коментарі до коду, де це необхідно
- [ ] Мої зміни не генерують нових попереджень
- [ ] Я додав тести, які підтверджують, що моя зміна працює
- [ ] Новий та існуючий код unit тести проходять локально
```

## 🏗 Структура проєкту

### Основні директорії

- `app/` - Next.js App Router (routes, layouts, pages)
- `components/` - React компоненти
  - `ui/` - shadcn/ui компоненти
- `lib/` - Утиліти та допоміжні функції
- `public/` - Статичні файли
- `hooks/` - Custom React hooks

### Додавання нового провайдера

1. Додайте конфігурацію в `lib/providers/config.ts`
2. Додайте обробку в `app/api/generate-code/route.ts`
3. Оновіть `components/provider-selector.tsx`
4. Додайте документацію

### Додавання нової функції

1. Створіть компонент у `components/`
2. Додайте необхідні API routes у `app/api/`
3. Оновіть типи та інтерфейси
4. Додайте документацію

## 🧪 Тестування

### Запуск тестів

```bash
npm test
```

### Написання тестів

- Використовуйте Jest та React Testing Library
- Покривайте критичні функції тестами
- Додавайте тести для нових функцій

## 📚 Документація

### Оновлення документації

- Оновлюйте README.md для нових функцій
- Додавайте коментарі до складного коду
- Оновлюйте API документацію

### Створення нової документації

1. Створіть файл у `docs/`
2. Додайте посилання в README.md
3. Використовуйте Markdown формат

## ❓ Питання?

Якщо у вас є питання:

- Відкрийте [Discussion](https://github.com/BOHDANMARCEN/-Arium-WebGen-AI-Powered-Local-HTML-CSS-JS-Generator/discussions)
- Створіть [Issue](https://github.com/BOHDANMARCEN/-Arium-WebGen-AI-Powered-Local-HTML-CSS-JS-Generator/issues)

## 🙏 Дякуємо!

Ваш внесок робить Arium WebGen кращим для всіх. Дякуємо за час та зусилля!

